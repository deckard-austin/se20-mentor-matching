// This page specific to a mentor, id being a query param passed through Airtable?
// Each mentor will map through the 10 startups and toggle a yes / no radio button
// When submitted, inputs match into Airtable. Only submit once.
import StartupList from '../../components/StartupList';
const Startups = () => <StartupList />;

export default Startups;
