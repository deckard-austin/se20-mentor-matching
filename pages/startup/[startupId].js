// This page specific to a startup, id being a query param passed through Airtable?
// Each startup will map through a list of mentors that have 'matched' with them
// and toggle a yes / no radio button
// When submitted, inputs match into Airtable

const MentorList = () => (
  // Map through here
  <div>Mentor List here</div>
);

export default MentorList;
